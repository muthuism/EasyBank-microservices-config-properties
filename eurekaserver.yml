server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  registerWithEureka: false
  fetchRegistry: false
  serviceUrl:
    defaultZoneL: http://${eureka.instance.hostname}:${server.port}/eureka/
