server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  registerWithEureka: false
  fetchRegistry: false
  serviceUrl:
    defaultZoneL: http://${eureka.instance.hostname}:${server.port}/eureka/
